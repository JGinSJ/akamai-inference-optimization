[workspace]
members  = ["proxy", "health"]
resolver = "2"

# Applied to all crates in the workspace.
# opt-level = "s" minimises Wasm binary size; cold-start time is
# sensitive to artifact size in the Fermyon runtime.
[profile.release]
opt-level = "s"
strip     = true
